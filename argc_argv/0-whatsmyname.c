#include <stdio.h>

/**
 * main - entry
 * @argv: argument vector - pointer to array of strings
 * Return: 0 (success)
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
