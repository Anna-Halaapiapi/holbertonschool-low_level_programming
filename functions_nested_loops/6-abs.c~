#include "main.h"

/**
 * _abs - computes absolute value of an integer
 * @n: number to check
 * Return: 0
 */

/* processes abs on var n */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
		else
		return (n);
	}
	return (0);
}
